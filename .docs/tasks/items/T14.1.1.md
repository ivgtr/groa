# T14.1.1: CLI基盤・init・configコマンド

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R14 / F14.1

## 目的
Commander.jsによるCLI基盤と、設定ファイル生成・表示コマンドを実装する

## 完了条件
- [x] Commander.js による CLI エントリポイントが実装されている
- [x] `groa init` で groa.json の雛形が生成される（バックエンド選択を含む）
- [x] `groa config` で現在の設定が表示される
- [x] 共通オプション（--backend, --force 等）が定義されている
- [x] バージョン表示（--version）が動作する
- [x] 不正なコマンドや引数に対してヘルプが表示される
- [x] ユニットテストでコマンド解析が検証されている

## 依存関係
- 先行: T2.1.2
- 後続: T14.1.2, T14.1.3, T14.1.4

## 実装メモ
- spec.md §7.1 に準拠
- Commander.js の program.command() でサブコマンドを定義
