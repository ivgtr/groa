# T18.1.2: SessionParams・PersonaContext型の追加

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: S
- **対応要件**: R5（§5.2）

## 目的
セッション実行パラメータとプロファイルコンテキストの型を定義する

## 完了条件
- [x] `packages/generate/src/types.ts` に以下を定義:
  - `SessionParams` (`mode`, `topic`, `temperature`, `maxLength`, `maxTurns`, `autoTurnLimit`, `numVariants`, `styleHint`)
  - `PersonaContext` (`buildName`, `persona`, `taggedTweets`, `embeddingIndex`)
- [x] `packages/generate/src/index.ts` にエクスポート追加
- [x] 旧 `GenerateParams` は残置（段階的移行）
- [x] 型エラーが0件

## 依存関係
- 先行: T18.1.1
- 後続: T18.2.1

## 実装メモ
- design-spec.md §4.7 に準拠
- `SessionParams.mode` は `@groa/types` の `SessionMode` を使用
