# T18.5.1: CLI generate サブコマンド体系への移行

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R14（§7.1）

## 目的
`groa generate` をサブコマンド体系に再構築し、4モードのCLIコマンドを実装する

## 完了条件
- [x] `packages/cli/src/commands/generate.ts` を親コマンド化（サブコマンド登録のみ）
- [x] `packages/cli/src/commands/generate-tweet.ts`: `groa generate tweet <name> <topic>` を実装
  - 既存の `runGenerateCommand` と等価な動作（SessionParams.mode="tweet"）
  - `--n`, `--temp`, `--max-length`, `--style-hint` オプション
- [x] `packages/cli/src/commands/generate-converse.ts`: `groa generate converse <name> <topic>` を実装
  - `--turns`, `--auto-limit`, `--temp`, `--max-length`, `--no-eval` オプション
- [x] `packages/cli/src/commands/generate-multi.ts`: `groa generate multi <name1> <name2> [names...]` を実装
  - `--topic`, `--turns`, `--temp`, `--max-length` オプション
- [x] `packages/cli/src/commands/generate-chat.ts`: `groa generate chat <name>` を実装
  - readline ベースの入力ループ
  - `--temp`, `--max-length` オプション
  - Ctrl+C / "quit" でセッション終了、ログ保存
- [x] ビルドアーティファクト読み込みロジックを共通ヘルパーとして抽出
- [x] `packages/cli/src/index.ts` にエクスポート追加
- [x] 既存の `groa generate <name> <topic>` は `groa generate tweet <name> <topic>` にリダイレクト or 削除
- [x] ユニットテスト（各サブコマンドの引数パース・実行フロー検証）
- [x] 型エラーが0件

## 依存関係
- 先行: T18.4.1
- 後続: T18.6.1

## 実装メモ
- spec.md §7.1 に準拠
- Commander.js の `.addCommand()` でサブコマンドを追加
- 各サブコマンドは `runSession()` パイプラインを呼び出す
