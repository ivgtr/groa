# T18.4.1: セッションパイプライン（runSession）・SessionStore実装

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R5（§5.2）

## 目的
パイプライン層にセッション実行のオーケストレーションとログ永続化を実装する

## 完了条件
- [x] `packages/pipeline/src/run-session.ts` に `runSession()` パイプラインを実装
  - Embedder作成 → LLMバックエンド作成 → セッション実行 → 評価 → ログ保存
  - PipelineProgress によるステップ進捗通知
  - CostLimitGuard によるコスト上限制御
- [x] `packages/pipeline/src/session-store.ts` に `SessionStore` を実装
  - `save(session)`: `.groa/sessions/{sessionId}.json` に保存
  - `load(sessionId)`: セッション読み込み
  - `list(filter?)`: セッション一覧（buildName / mode でフィルタ可能）
  - APIキーパターン検出・サニタイズ
- [x] `packages/pipeline/src/index.ts` にエクスポート追加
- [x] 既存の `run-generate.ts` は残置（段階的移行）
- [x] ユニットテスト（SessionStore の読み書き・一覧を検証）
- [x] 型エラーが0件

## 依存関係
- 先行: T18.2.1, T18.3.1
- 後続: T18.5.1

## 実装メモ
- sessionId の形式: `{mode}-{YYYYMMDD}-{6桁hex}`
- ログ保存パスはビルドキャッシュとは独立（`.groa/sessions/`）
- APIキー検出は `StepCacheManager` と同等のロジックを使用
