# T18.5.2: 設定スキーマの拡張（autoTurnLimit）

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: S
- **対応要件**: R8（§8.1）

## 目的
`groa.config.json` の generate ステップ設定に `autoTurnLimit` を追加する

## 完了条件
- [x] `packages/config/src/schema.ts` の `GenerateStepSchema` に `autoTurnLimit: z.number().default(8)` を追加
- [x] デフォルト値の定数 `StepsSchema.default()` にも反映
- [x] 既存のテストが通る
- [x] 型エラーが0件

## 依存関係
- 先行: なし
- 後続: T18.2.1

## 実装メモ
- spec.md §8.1 に準拠
- 既存の `defaultTemperature`, `maxLength`, `numVariants` と同列に追加
