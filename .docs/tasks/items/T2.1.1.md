# T2.1.1: groa.json Zodスキーマ・デフォルト値・バリデーション

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R2 / F2.1

## 目的
groa.json の設定項目をZodスキーマで定義し、デフォルト値とバリデーションを提供する

## 完了条件
- [x] spec.md §8.1 の全設定項目がZodスキーマで定義されている
- [x] デフォルト値が spec.md §8.1 の表と一致する
- [x] backend, models, steps, cacheDir, costLimitUsd の各項目が正しくバリデーションされる
- [x] 工程別オーバーライド（steps.{stepName}.model, steps.{stepName}.apiKey）が定義されている
- [x] 設定ファイルが存在しない場合にデフォルト値で動作する
- [x] 不正な設定値に対してわかりやすいエラーメッセージを返す
- [x] 型エラーが0件

## 依存関係
- 先行: T1.1.1
- 後続: T2.1.2, T3.1.1

## 実装メモ
- Zod v4 では `.default({})` に空オブジェクトを渡せないため、ファクトリ関数 `.default(() => ({...}))` パターンを使用
- steps.stats はパラメータなし（空オブジェクト）
