# T3.2.2: Batch API 対応

- **ステータス**: Done
- **優先度**: Should
- **サイズ**: M
- **対応要件**: R3 / F3.2

## 目的
Anthropic Batch APIによる非同期バッチ処理を実装し、Step 2の分類コストを50%削減する

## 完了条件
- [x] Batch APIへのリクエスト投入が動作する
- [x] ポーリング間隔30秒で結果を取得する
- [x] 最大待機時間60分でタイムアウトエラーを返す
- [x] バッチ結果のパースとエラーハンドリングが実装されている
- [x] api バックエンド時のみ使用される（claude-code時は逐次実行にフォールバック）
- [x] ユニットテストで投入→ポーリング→結果取得のフローが検証されている

## 依存関係
- 先行: T3.2.1
- 後続: T6.1.1

## 実装メモ
- spec.md §9.2 のBatch API応答待機仕様に準拠
- ポーリング中の進捗表示をコールバックで通知
- BatchClient クラスで submitAndWait（コア）と submitWithRetry（部分失敗リトライ B-9対応）を提供
- custom_id 設計: `{stepName}-{batchIndex}-{itemIndex}` 形式（T6.1.1 で利用）
- コスト計算の Batch 50%割引は cost.ts で既実装済み
