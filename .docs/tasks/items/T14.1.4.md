# T14.1.4: inspect・cost・cleanコマンド・UX

- **ステータス**: Todo
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R14 / F14.1

## 目的
ユーティリティコマンド群とCLI全体のユーザー体験を実装する

## 完了条件
- [ ] `groa inspect` で PersonaDocument の内容が表示される
- [ ] `groa cost` で累計コストが表示される
- [ ] `groa clean` で全キャッシュが削除される
- [ ] `groa clean --step <name>` で特定ステップのキャッシュが削除される
- [ ] エラーメッセージに「ユーザーが次にとるべきアクション」が含まれる
- [ ] 入力バリデーション（ファイル存在, JSON形式, ツイート件数範囲）が統一的に処理される
- [ ] ユニットテストで各コマンドが検証されている

## 依存関係
- 先行: T14.1.1, T13.2.1
- 後続: なし

## 実装メモ
- spec.md §7.1 に準拠
