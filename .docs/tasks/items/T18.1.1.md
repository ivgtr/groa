# T18.1.1: Session型定義の追加

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R5（§2.3, §5.2）

## 目的
全テキスト生成の統一単位となる Session 型体系を `packages/types` に追加する

## 完了条件
- [x] `packages/types/src/session.ts` に以下のZodスキーマ＋型を定義:
  - `SessionMode` (`"tweet" | "converse" | "multi" | "chat"`)
  - `SessionParticipant` (`buildName`, `role`)
  - `SessionTurn` (`index`, `speakerId`, `text`, `fewShotIds`, `modelUsed`, `timestamp`)
  - `SessionEvaluation` (`authenticity`, `coherence`, `consistency`, `rationale`)
  - `Session` (`id`, `mode`, `topic`, `participants`, `turns`, `evaluation`, `createdAt`, `completedAt`)
- [x] `packages/types/src/index.ts` に上記のエクスポートを追加
- [x] 既存の `GeneratedText` / `EvaluationResult` は残置（後続タスクで段階的に移行）
- [x] `pnpm build` が通る
- [x] 型エラーが0件

## 依存関係
- 先行: なし
- 後続: T18.1.2, T18.2.1, T18.3.1, T18.4.1

## 実装メモ
- Branded Types（`TweetId`, `Timestamp`, `ModelIdString`）を活用
- Zodスキーマと型定義の一体化（`z.infer`で型導出）
- spec.md §2.3、design-spec.md §2.5 に準拠
