# T14.1.2: build・stepコマンド

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R14 / F14.1

## 目的
ビルドフェーズの一括実行コマンドと個別ステップ実行コマンドを実装する

## 完了条件
- [x] `groa build <tweets.json>` で Step 0-5 が一括実行される
- [x] `groa build <tweets.json> --backend claude-code` でバックエンド一時指定が動作する
- [x] `groa step preprocess <tweets.json>` で個別ステップが実行される
- [x] `groa step stats / classify / analyze / synthesize / embed` が動作する
- [x] 進捗表示が spec.md §7.1 のフォーマットで出力される
- [x] --force オプションでキャッシュスキップが無視される
- [x] 初回実行時にデータ送信確認のインタラクティブ確認が表示される
- [x] ユニットテストでコマンド実行フローが検証されている

## 依存関係
- 先行: T14.1.1, T13.1.1, T13.2.2
- 後続: なし

## 実装メモ
- spec.md §7.1 に準拠
- 入力JSONファイルの存在チェック・フォーマットバリデーションを実行
