# T18.2.3: 自動継続判断（continuation）実装

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: S
- **対応要件**: R5（§5.2）

## 目的
converse/multiモードで「会話を続けるべきか」をLLMで自動判断する

## 完了条件
- [x] `packages/generate/src/prompt/continuation.ts` に `shouldContinue()` を実装
- [x] quickモデル（temperature 0.0）で `{ "shouldContinue": boolean }` を返す
- [x] JSONパース失敗時は `true` にフォールバック（途中打切りより続行が安全）
- [x] `autoTurnLimit` 超過時は強制 `false`
- [x] ユニットテスト（モックLLMで正常系・失敗系を検証）
- [x] 型エラーが0件

## 依存関係
- 先行: T18.1.1
- 後続: T18.2.1

## 実装メモ
- quickモデルの解決は `resolveStepConfig(config, "classify")` 相当（quickティア）
- メタプロンプト: 直近の会話ターンを入力し、会話の自然な終了を判断
