# T17.3.2: groa config set サブコマンド追加

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M

## 目的
`groa config set <key> <value>` コマンドで groa.json の設定値を更新できるようにする。

## 完了条件
- [x] `groa config set models.sonnet <id>` で groa.json の `models.sonnet` が更新される
- [x] `groa config set models.haiku <id>` で `models.haiku` が更新される
- [x] `groa config set models.opus <id>` で `models.opus` が更新される
- [x] `groa config set backend <type>` で `backend` が更新される
- [x] ドット記法キー（`models.sonnet`, `apiKeys.anthropic` 等）が正しくネストされたJSONパスに解決される
- [x] 更新後に `GroaConfigSchema.parse()` でバリデーションが実行される
- [x] 不正なキーや値に対してわかりやすいエラーメッセージを返す
- [x] `groa.json` が存在しない場合に `groa init` の実行を促すエラーを返す
- [x] `groa config`（引数なし）は既存の表示動作を維持する（後方互換）
- [x] ユニットテストで正常系・バリデーションエラー系が検証されている
- [x] 型エラーが0件
- [x] テストが通る

## 依存関係
- 先行: T17.3.1
- 後続: なし

## 実装メモ
- `setNestedValue` ユーティリティを config.ts 内にインライン実装する（外部依存不要）
- Commander.js の `config.addCommand(new Command("set"))` でサブコマンドを追加
- `config.action()` で引数なし時のデフォルト動作（表示）を維持
