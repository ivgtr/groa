# T17.1.1: BackendType 再定義・ModelsSchema デフォルト値撤廃

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M

## 目的
`BackendType` を接続先の実体に合わせて再定義し、モデルIDのハードコーディングを撤廃する。
この変更は後続タスクの基盤となる破壊的変更。

## 完了条件
- [x] `BACKENDS` が `["anthropic", "openrouter", "claude-code"]` に変更されている
- [x] `ApiKeysSchema` に `openrouter: z.string().optional()` が追加されている
- [x] `ModelsSchema` の `haiku` / `sonnet` / `opus` が `z.string().nullable().default(null)` に変更されている（デフォルトモデルIDが撤廃されている）
- [x] `resolve.ts` の `config.backend === "api"` が `config.backend === "anthropic"` に変更されている
- [x] `resolveApiKey` が `backend === "openrouter"` 時に `apiKeys.openrouter` / `OPENROUTER_API_KEY` を参照する
- [x] `resolveModel` がモデル未設定（`null`）時に明示的なエラーメッセージを throw する
- [x] `api-backend.ts` の `backendType()` が `"anthropic"` を返す
- [x] `factory.ts` の switch が `"anthropic"` で既存 `ApiBackend` を生成し、`"openrouter"` は未実装エラーを throw する
- [x] 既存テスト（schema.test.ts, resolve.test.ts, api-backend.test.ts, factory.test.ts 等）が新しい BackendType・ModelsSchema に合わせて更新されている
- [x] 型エラーが0件
- [x] テストが通る

## 依存関係
- 先行: なし（既存タスクはすべて Done）
- 後続: T17.1.2, T17.2.1, T17.3.1

## 実装メモ
- `createDefaultConfig()` はモデルIDを含まない設定を返すようになる
- 既存の `groa.json`（モデルID記述済み）は Zod の nullable が文字列を受け入れるため後方互換
- `"openrouter"` の factory ケースは T17.2.1 で実装するため、ここでは `throw new Error("OpenRouter バックエンドは未実装です")` とする

## レビュー残課題
- `factory.test.ts` が存在しない。`createLlmBackend` の各バックエンド分岐と不正値の exhaustive check をカバーするユニットテストの追加を検討。現状は pipeline テストで間接的にカバーされている
