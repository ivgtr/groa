# T18.6.1: 旧API（GeneratedText / runGenerate）の移行・削除

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R5

## 目的
旧APIを新Session APIに完全移行し、不要になったコードを削除する

## 完了条件
- [x] `packages/types/src/output.ts` から `GeneratedText` / `EvaluationResult` を削除
- [x] `packages/generate/src/generate.ts` / `generate-prompt.ts` を削除
- [x] `packages/evaluate/src/evaluate.ts` / `evaluate-prompt.ts` を削除
- [x] `packages/pipeline/src/run-generate.ts` を削除
- [x] `packages/cli/src/commands/generate.ts` から旧 `runGenerateCommand` を削除
- [x] 全パッケージの `index.ts` からの旧エクスポートを削除
- [x] Web側（`packages/web/`）の `GeneratedText` 参照を `Session` に更新
- [x] 全既存テストを新APIに移行
- [x] `pnpm build` が通る
- [x] `pnpm test` が通る
- [x] 型エラーが0件

## 依存関係
- 先行: T18.5.1（全新APIが実装済みであること）
- 後続: なし

## 実装メモ
- 破壊的変更。旧APIの利用箇所を全て洗い出してから一括移行
- Web側の GenerateView コンポーネントも Session 型に対応させる
