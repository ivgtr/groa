# T16.2.2: セキュリティ対応

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R16 / F16.2

## 目的
APIキー保護、ログセキュリティ、データ送信確認の横断的セキュリティ要件を実装する

## 完了条件
- [x] ログ出力時にAPIキーとAuthorizationヘッダの値がマスクされる
- [x] 中間結果JSONにAPIキーが書き出されない
- [x] groa.json にAPIキーが直接記述されている場合、Unix環境で0600以外のパーミッションなら警告が出る
- [x] CLI: 初回実行時にツイートデータがLLM APIに送信される旨のインタラクティブ確認が表示される
- [x] Web: 初回利用時に同意ダイアログが表示される
- [x] ユニットテストでマスク処理が検証されている

## 依存関係
- 先行: T3.1.1, T2.1.2
- 後続: なし

## 実装メモ
- spec.md §9.3 に準拠
- マスク処理はログレベルに関わらず全箇所に適用
- ログマスク: `maskSensitiveValues()` (llm-client) - APIキーパターン + HTTPヘッダのマスク
- 中間結果保護: `stripSensitiveData()` + `assertNoApiKeys()` (pipeline/cache.ts、T13.2.1で実装済み)
- パーミッション警告: `checkConfigPermissions()` (config/resolve.ts、T2.1.2で実装済み)
- CLI同意確認: `ensureConsent()` (cli/consent.ts) - promptFn 差し替えでテスト可能
- Web同意ダイアログ: T15.1.1 (Webセットアップ) 完了後に実装予定。consent のインターフェースは CLI と共通化可能
