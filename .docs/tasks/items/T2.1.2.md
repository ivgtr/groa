# T2.1.2: 設定解決ロジック（優先順位・環境変数・パーミッション警告）

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: M
- **対応要件**: R2 / F2.1

## 目的
バックエンド種別に応じた設定値の解決優先順位と、セキュリティ警告機能を実装する

## 完了条件
- [x] api バックエンド: 工程別指定 → グローバル設定 → 環境変数 の優先順位で解決される
- [x] claude-code バックエンド: 工程別model → models.{tier} → Claude Codeデフォルト の優先順位で解決される
- [x] 環境変数 ANTHROPIC_API_KEY が設定解決に反映される
- [x] groa.json にAPIキーが直接記述されている場合、Unix環境で0600以外のパーミッションなら警告を出す
- [x] モデルティア（haiku/sonnet/opus）から具体的なモデルIDへの解決が行われる
- [x] ユニットテストで各優先順位パターンが検証されている

## 依存関係
- 先行: T2.1.1
- 後続: T3.1.1, T14.1.1
