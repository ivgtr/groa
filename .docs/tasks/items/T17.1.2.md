# T17.1.2: api-backend → anthropic-backend リネーム

- **ステータス**: Done
- **優先度**: Must
- **サイズ**: S

## 目的
`api-backend.ts` を `anthropic-backend.ts` にリネームし、クラス名・エクスポート名を Anthropic 固有であることを明示する名称に変更する。

## 完了条件
- [x] `packages/llm-client/src/api-backend.ts` が `anthropic-backend.ts` にリネームされている
- [x] `ApiBackend` クラスが `AnthropicBackend` にリネームされている
- [x] `ApiError` クラス名はそのまま維持する（汎用エラーとして他バックエンドでも利用するため）
- [x] `packages/llm-client/src/index.ts` のエクスポート名が更新されている
- [x] `factory.ts` の import が更新されている
- [x] `api-backend.test.ts` が `anthropic-backend.test.ts` にリネームされ、import が更新されている
- [x] 他パッケージから `ApiBackend` を参照している箇所がすべて `AnthropicBackend` に更新されている
- [x] 型エラーが0件
- [x] テストが通る

## 依存関係
- 先行: T17.1.1
- 後続: T17.2.1
